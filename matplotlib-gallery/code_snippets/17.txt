# NOTE: Please import relevant libraries (e.g., import matplotlib.pyplot as plt, import numpy as np) before running this snippet.

x = np.linspace(0, 5, 100)
y = np.exp(x)

plt.figure()
plt.plot(x, y)
plt.yscale("log")
plt.title("Log Scale Example")
plt.grid(True)
plt.show()